# Database (auto-configured by docker-compose, change for external DB)
DATABASE_URL=postgresql://ironlab:ironlab123@db:5432/ironlab

# JWT — CHANGE THIS in production!
JWT_SECRET=ironlab_super_secret_change_in_production

# API server port (internal, exposed via docker-compose)
PORT=3001
