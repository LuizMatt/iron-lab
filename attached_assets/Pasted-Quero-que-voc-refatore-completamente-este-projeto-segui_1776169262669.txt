Quero que você refatore completamente este projeto seguindo as instruções abaixo. Não faça mudanças parciais — aplique tudo de forma consistente e moderna.

🔴 Objetivo geral

Reestruturar o projeto com:

Frontend moderno usando Nuxt 3 (última versão estável)
Backend em Node.js + Express
Banco de dados PostgreSQL
Arquitetura limpa, segura e escalável
📁 Estrutura obrigatória

Organize o projeto assim:

/server     → backend (Node + Express + PostgreSQL)
/frontend   → frontend (Nuxt 3)
Remover qualquer estrutura antiga que conflite
Não misturar frontend e backend
🔴 Frontend (RECRIAR DO ZERO)

O frontend atual (React + Vite) deve ser removido completamente.

Criar novo projeto com:

Nuxt 3 (última versão)
Vue 3 (Composition API)
Estrutura padrão:
/pages
/components
/composables
/plugins
/middleware
Requisitos do frontend:
Implementar file-based routing do Nuxt
Converter todas as páginas existentes para pages/
Criar componentes reutilizáveis em components/
Usar useFetch ou client gerado via Orval (com fetch nativo ou Vue Query)
Configurar variáveis de ambiente corretamente
🔴 Autenticação (CRÍTICO — corrigir segurança)

Atualmente usa localStorage → isso deve ser removido.

Implementar autenticação segura com:

Cookies httpOnly
Token JWT:
gerado no backend
enviado via cookie
inacessível no frontend
Fluxo esperado:
Login → backend seta cookie
Frontend apenas consome sessão
Middleware no Nuxt para proteger rotas
Logout limpando cookie
🟠 Backend — Arquitetura correta

Refatorar backend para padrão em camadas:

routes → controllers → services → repositories (ou ORM)
Regras:
routes/ → apenas rotas
controllers/ → req/res
services/ → regras de negócio
repositories/ ou ORM → acesso ao banco
🟠 Banco de dados (PostgreSQL)
Garantir integração com PostgreSQL
Usar ORM (preferência: Prisma ou Sequelize)
Organizar models corretamente
Separar lógica de acesso ao banco da lógica de negócio
🟠 Tratamento global de erros

Implementar middleware global no Express:

Formato:

(err, req, res, next)
Deve:
Padronizar erros
Evitar vazamento de stack trace
Retornar sempre JSON consistente

Exemplo:

{
  "error": true,
  "message": "Descrição do erro"
}
⚠️ Regras obrigatórias
NÃO usar localStorage para auth
NÃO deixar lógica dentro de rotas
NÃO misturar React com Nuxt
NÃO manter código legado desnecessário
Seguir boas práticas de organização
✅ Resultado esperado
Projeto dividido em /server e /frontend
Frontend moderno com Nuxt 3 funcionando
Backend estruturado e escalável
PostgreSQL integrado corretamente
Autenticação segura com cookies httpOnly
Middleware global de erro funcionando
💡 (opcional, mas recomendado)
Usar TypeScript em todo o projeto
ESLint + Prettier configurados
Docker (se possível)